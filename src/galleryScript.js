function showText(text) {

    // Get the HTML element to display the text
    var choiceText = document.getElementById("choiceText");

    // Display the text
    choiceText.textContent = text;
    }
